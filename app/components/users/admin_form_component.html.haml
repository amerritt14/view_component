.mt-3
  = form_with model: user, local: true, builder: MaterialForm do |form|
    = form.text_field :first_name
    = form.text_field :last_name
    = form.select :role, User::ROLES
    = form.submit
